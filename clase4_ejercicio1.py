
tiempo = {}

for i in range(0, len(datos_tiempo['time'])):
    tiempo[datos_tiempo['time'][i]] = {

    }

    has_maxTemp=False
    has_minTemp=False

    for key, value in datos_tiempo.items():
        tiempo[datos_tiempo['time'][i]] [key]= value [i]

    else 

for fecha, info in tiempo: 
    if "temperature_2m_max" in info and "temperature_2m_min"in info :
        info[]

import json
print(json.dumps(tiempo, indent=2))



